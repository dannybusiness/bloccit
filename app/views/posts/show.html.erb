<h1><%= @post.title %></h1>

<div class="row">
  <div class="span8">
    <p><%= @post.body %>
  </div>
  <div class="span4">
  <% if can? :edit, @post %>
    <%= link_to "Edit", edit_topic_post_path(@topic,@post), class: 'btn btn_large btn-black' %>
  <% end %>
  </div>
</div>